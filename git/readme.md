# add git to manage code clone,push.pull
# 在github上点击创建仓库
# git init
# git add .
# git commit
# 在github创建仓库
# git remote add origin http:
# git push origin master
# git reset --hard (commit id) 返回到想回的版本